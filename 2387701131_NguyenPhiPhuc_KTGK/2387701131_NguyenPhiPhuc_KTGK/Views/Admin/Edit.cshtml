@model _2387701131_NguyenPhiPhuc_KTGK.Models.Course
@{
    ViewData["Title"] = "Chỉnh sửa học phần";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="form-card">
                <h2 class="form-title"><i class="fas fa-edit"></i> Chỉnh sửa học phần</h2>
                <form asp-action="Edit" method="post" enctype="multipart/form-data">
                    <input type="hidden" asp-for="Id" />
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                    <div class="form-floating mb-3">
                        <input asp-for="Name" class="form-control" placeholder="Tên học phần" />
                        <label asp-for="Name">Tên học phần</label>
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-floating mb-3">
                        <input asp-for="Credits" class="form-control" placeholder="Số tín chỉ" type="number" />
                        <label asp-for="Credits">Số tín chỉ</label>
                        <span asp-validation-for="Credits" class="text-danger"></span>
                    </div>
                    <div class="form-floating mb-3">
                        <input asp-for="Lecturer" class="form-control" placeholder="Giảng viên" />
                        <label asp-for="Lecturer">Giảng viên</label>
                        <span asp-validation-for="Lecturer" class="text-danger"></span>
                    </div>
                    <div class="form-floating mb-3">
                        <select asp-for="CategoryId" class="form-control" asp-items="@(ViewBag.Categories as SelectList)">
                            <option value="">-- Chọn danh mục --</option>
                        </select>
                        <label asp-for="CategoryId">Danh mục</label>
                        <span asp-validation-for="CategoryId" class="text-danger"></span>
                    </div>
                    @if (!string.IsNullOrEmpty(Model.Image))
                    {
                        <div class="mb-3">
                            <label class="form-label">Hình ảnh hiện tại</label>
                            <div>
                                <img src="@Model.Image" alt="Current" class="img-thumbnail" style="max-height: 150px;" />
                            </div>
                        </div>
                    }
                    <div class="mb-3">
                        <label class="form-label">Thay đổi hình ảnh</label>
                        <input type="file" name="imageFile" class="form-control" accept="image/*" />
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-warning btn-lg"><i class="fas fa-save"></i> Cập nhật</button>
                        <a asp-action="Courses" class="btn btn-secondary btn-lg"><i class="fas fa-arrow-left"></i> Quay lại</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
